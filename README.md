
  # poprawione filtry (Copy)

  This is a code bundle for poprawione filtry (Copy). The original project is available at https://www.figma.com/design/xFaflk0VBFutvV87nTRQ8i/poprawione-filtry--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  